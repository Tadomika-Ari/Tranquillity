name: La Mira

on:
  push:
    branches:
      - main
      - 19-worflows
  pull_request:
    branches:
      - main
      - 19-worflows

jobs:
  check_work:
    runs-on: ubuntu-latest
    name: check_work
    if: github.repository != 'EpitechPGE1-2025/G-PSU-200-NCE-2-1-42sh-2'
    container:
      image: python:3.14
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
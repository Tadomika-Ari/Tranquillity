name: La Mira

on:
  push:
    branches:
      - main
      - 19-worflows
  pull_request:
    branches:
      - main
      - 19-worflows

jobs:
  check_work:
    runs-on: ubuntu-latest
    name: check_work
    container:
      image: python:3.14
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0